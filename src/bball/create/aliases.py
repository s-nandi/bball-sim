from bball.ball import Ball
from bball.game import GameSettings
from bball.space import Space

# Aliases for consistency while still enabling type hinting
create_ball = Ball  # pylint: disable=invalid-name
create_space = Space  # pylint: disable=invalid-name
create_game_settings = GameSettings  # pylint: disable=invalid-name
